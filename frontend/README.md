# LDAP 运维管理系统 - 前端

基于 Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router 4。

## 环境要求

- Node.js 18+
- npm / pnpm / yarn

## 安装与运行

```bash
# 安装依赖
npm install

# 开发
npm run dev

# 构建
npm run build

# 预览构建结果
npm run preview
```

开发时默认代理 `/api` 到 `http://localhost:8080`，需后端服务已启动。
